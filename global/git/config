# vim:syntax=gitconfig

[core]
	editor = vim
    include = ./org/config
[diff]
	tool = vimdiff
[merge]
	tool = vimdiff
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
